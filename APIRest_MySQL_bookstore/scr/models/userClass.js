

class User {

    constructor(id_user, name, last_name, email, photo, password){
        id_user   = id_user,
        name      = name,
        last_name = last_name,
        email     = email,
        photo     = photo,
        password  = password 
    }
}

module.exports = {User};